# lksoftGwebsrv — пример переменных окружения
#
# ВАЖНО:
# - Не коммитьте реальные секреты в репозиторий.
# - В production задавайте переменные через systemd/docker/k8s/CI secrets.

# --- App ---
IGS_APP_DEBUG=0
IGS_APP_TIMEZONE=Asia/Yekaterinburg
IGS_BASE_URL=

# --- Uploads ---
# Рекомендуется вынести uploads вне публичного web-root и отдавать через контроллер с проверкой прав.
IGS_UPLOAD_PATH=/var/www/lksoftGwebsrv/uploads
IGS_UPLOAD_MAX_BYTES=52428800

# --- Database (PostgreSQL) ---
IGS_DB_HOST=localhost
IGS_DB_PORT=5432
IGS_DB_NAME=lksoftgwebsrv
IGS_DB_USER=lksoftgwebsrv
IGS_DB_PASSWORD=CHANGE_ME
IGS_DB_CHARSET=UTF8

# --- CORS ---
# Для production: перечислите разрешённые origin через запятую.
# Пример: IGS_CORS_ALLOW_ORIGINS=https://igs.example.com,https://office.example.com
IGS_CORS_ALLOW_ORIGINS=*
IGS_CORS_MAX_AGE=86400

# --- Session (на будущее, если будет cookie-based auth) ---
IGS_SESSION_LIFETIME=86400
IGS_SESSION_SECURE=1

# --- JWT (на будущее, если будет JWT) ---
IGS_JWT_SECRET=
IGS_JWT_ALG=HS256
IGS_JWT_EXP=86400

